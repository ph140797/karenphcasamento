{
  "name": "karen-ph-casamento",
  "version": "1.0.0",
  "private": true,
  "description": "Wedding website for Karen and Paulo Henrique with Asaas Checkout and gift admin APIs.",
  "scripts": {
    "build": "npm run check",
    "dev": "netlify dev",
    "start": "netlify dev",
    "migrate": "node scripts/migrate-supabase.js",
    "seed:gifts": "node scripts/seed-gifts.js",
    "seed:gifts:sql": "node scripts/seed-gifts.js --sql",
    "check": "node --check scripts/migrate-supabase.js && node --check scripts/seed-gifts.js && for f in netlify/functions/*.js; do node --check \"$f\" || exit 1; done"
  },
  "dependencies": {
    "@netlify/blobs": "^8.2.0",
    "@supabase/supabase-js": "^2.45.4",
    "dotenv": "^16.4.5",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.36.2"
  }
}
